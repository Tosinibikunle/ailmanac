---
sidebar_position: 6
title: "Permisos y modos de permiso"
description: "Reglas allow/ask/deny y modos: reduce las preguntas sin perder seguridad."
---

<LevelBadge level="intermediate" />

<VerifyNote lastVerified="2026-06-20" source="https://code.claude.com/docs/en/settings">
La sintaxis exacta de las reglas y los nombres de los modos evolucionan: confírmalos con la documentación oficial de settings/permisos de Claude Code.
</VerifyNote>

<Callout type="objectives" items={["Qué significan los tres veredictos de permiso (allow / ask / deny)", "Cómo una regla de permiso coincide con una herramienta más un patrón", "Los cuatro modos de permiso y cuándo usar cada uno", "Cómo construir una lista de permitidos inicial sensata que reduzca las preguntas sin perder seguridad", "Dónde guardar las reglas de permiso del proyecto frente a las personales"]} />

Los permisos deciden **qué puede hacer Claude Code sin detenerse a preguntarte**. Ajústalos bien y obtienes fluidez sin perder el control; ajústalos mal y o bien apruebas todo a ciegas o te ahogas en preguntas.

## Los tres veredictos

Cada posible acción se resuelve en uno de estos:

<Flashcards cards={[{front: "allow", back: "Hazlo, sin preguntar."}, {front: "ask", back: "Pausa y pregúntame: el valor por defecto para cualquier cosa no permitida ni denegada explícitamente."}, {front: "deny", back: "Nunca, ni aunque se pida."}]} />

Las reglas suelen coincidir con una herramienta más un patrón, p. ej. permitir `Bash(npm run test:*)` o denegar `Read(./.env)`.

<PromptCard title="Permitir una herramienta + patrón">{`Bash(npm run test:*)`}</PromptCard>

<PromptCard title="Denegar una herramienta + patrón">{`Read(./.env)`}</PromptCard>

## Modos de permiso

Un modo establece la postura general de una sesión:

| Modo | Comportamiento | Úsalo cuando |
|---|---|---|
| **default / ask** | Pregunta por cualquier cosa no preautorizada | Trabajo del día a día |
| **plan** | Solo lectura; propone, nunca actúa | Tareas grandes/arriesgadas — ver [Modo Plan](/docs/claude-code/plan-mode) |
| **acceptEdits** | Acepta automáticamente las ediciones de archivos | Una sesión de edición de confianza y bien acotada |
| **bypass / yolo** | Omite las preguntas por completo | Solo sandboxes/CI — **nunca** en una máquina con secretos |

<Callout type="warning" items={["El bypass pertenece a un sandbox. Ejecutar con todas las preguntas desactivadas en tu máquina real es la forma en que un agente acaba tocando algo que no debería. Resérvalo para entornos desechables. Consulta Endurecer ejecuciones autónomas en /docs/security/hardening-autonomous-runs."]} />

## Una lista de permitidos sensata para empezar

El objetivo: preautorizar lo seguro y repetitivo; mantener lo destructivo en **ask** o **deny**.

<Steps items={[{title: "Permite lo seguro + repetitivo", body: "Leer archivos, ejecutar tus comandos de test/lint/build, git status/diff."}, {title: "Pregunta antes de lo de riesgo medio", body: "Instalar dependencias, escribir archivos fuera del proyecto, llamadas de red."}, {title: "Deniega lo destructivo", body: "Leer archivos secretos (.env, archivos de claves), force-push, rm -rf."}]} />

Guarda las reglas del proyecto en [`settings.json`](/docs/claude-code/settings) (compartido) y las anulaciones personales en `settings.local.json`.

<Callout type="tip" items={["Deja que aprenda de tus preguntas: aprueba el mismo comando seguro unas cuantas veces y sabrás exactamente qué añadir a tu lista de permitidos — convirtiendo preguntas repetidas en una regla puntual."]} />

<Callout type="takeaways" items={["Tres veredictos: allow (sin preguntar), ask (por defecto — pausa y confirma), deny (nunca).", "Las reglas coinciden con una herramienta más un patrón, como Bash(npm run test:*) o Read(./.env).", "Cuatro modos establecen la postura de la sesión: default/ask, plan (solo lectura), acceptEdits, bypass/yolo (solo sandbox).", "Construye una lista de permitidos: permite lo seguro + repetitivo, pregunta para lo de riesgo medio, deniega lo destructivo.", "Las reglas compartidas van en settings.json; las anulaciones personales en settings.local.json."]} />

<Quiz title="Ponte a prueba" questions={[{q: "¿Qué hace el veredicto por defecto 'ask' ante una acción que no está explícitamente permitida ni denegada?", options: ["La ejecuta en silencio", "Pausa y te pide confirmación", "La bloquea de forma permanente"], answer: 1, explain: "ask es el valor por defecto para cualquier cosa no permitida/denegada explícitamente: pausa y te pregunta."}, {q: "¿Qué modo de permiso es seguro SOLO en sandboxes o CI, nunca en una máquina con secretos?", options: ["plan", "acceptEdits", "bypass / yolo"], answer: 2, explain: "bypass / yolo omite las preguntas por completo; resérvalo para entornos desechables — nunca en una máquina con secretos."}, {q: "¿Dónde deberías guardar las reglas de permiso compartidas del proyecto frente a las anulaciones personales?", options: ["Ambas en settings.local.json", "Las reglas del proyecto en settings.json; las anulaciones personales en settings.local.json", "Las reglas del proyecto en settings.local.json; las anulaciones personales en settings.json"], answer: 1, explain: "Las reglas del proyecto van en settings.json (compartido); las anulaciones personales van en settings.local.json."}, {q: "¿Cuál de estos pertenece a la lista de 'deny' en una lista de permitidos inicial sensata?", options: ["git status / diff", "rm -rf y leer .env", "Ejecutar tus comandos de test/lint/build"], answer: 1, explain: "Deniega lo destructivo: leer archivos secretos (.env, archivos de claves), force-push y rm -rf."}]} />

## Siguiente

- [settings.json: El sistema de configuración](/docs/claude-code/settings)
- [Hooks](/docs/claude-code/hooks) — aplica reglas de forma determinista, más allá de allow/deny
- [Seguridad y uso responsable](/docs/security/responsible-use)
