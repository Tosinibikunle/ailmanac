---
sidebar_position: 6
title: "Разрешения и режимы разрешений"
description: "Правила allow/ask/deny и режимы — меньше запросов без потери безопасности."
---

<LevelBadge level="intermediate" />

<VerifyNote lastVerified="2026-06-20" source="https://code.claude.com/docs/en/settings">
Точный синтаксис правил и имена режимов развиваются — сверяйтесь с официальной документацией Claude Code по настройкам/разрешениям.
</VerifyNote>

<Callout type="objectives" items={["Что означают три вердикта разрешений (allow / ask / deny)", "Как правила разрешений сопоставляются с инструментом плюс шаблоном", "Четыре режима разрешений и когда использовать каждый из них", "Как составить разумный стартовый allowlist, который сокращает запросы без потери безопасности", "Где хранить правила разрешений для проекта и личные"]} />

Разрешения решают, **что Claude Code может делать, не останавливаясь, чтобы спросить вас**. Настройте их хорошо — и получите поток без потери контроля; настройте плохо — и вы либо штампуете подтверждение всему подряд, либо тонете в запросах.

## Три вердикта

Каждое потенциальное действие сводится к одному из:

<Flashcards cards={[{front: "allow", back: "Выполнить, без запроса."}, {front: "ask", back: "Приостановиться и спросить вас — значение по умолчанию для всего, что явно не разрешено и не запрещено."}, {front: "deny", back: "Никогда, даже если попросят."}]} />

Правила обычно сопоставляются с инструментом плюс шаблоном, например allow `Bash(npm run test:*)` или deny `Read(./.env)`.

<PromptCard title="Разрешить инструмент + шаблон">{`Bash(npm run test:*)`}</PromptCard>

<PromptCard title="Запретить инструмент + шаблон">{`Read(./.env)`}</PromptCard>

## Режимы разрешений

Режим задаёт общую позицию для сессии:

| Режим | Поведение | Когда использовать |
|---|---|---|
| **default / ask** | Запрашивает всё, что не разрешено заранее | Повседневная работа |
| **plan** | Только для чтения; предлагает, никогда не действует | Большие/рискованные задачи — см. [Режим планирования](/docs/claude-code/plan-mode) |
| **acceptEdits** | Автоматически принимает правки файлов | Доверенная, хорошо ограниченная сессия редактирования |
| **bypass / yolo** | Полностью пропускает запросы | Только песочницы/CI — **никогда** на машине с секретами |

<Callout type="warning" items={["Bypass — это место в песочнице. Запуск с полностью отключёнными запросами на вашей реальной машине — это путь к тому, что агент тронет что-то, что не должен. Оставьте это для одноразовых сред. См. «Усиление защиты автономных запусков» по адресу /docs/security/hardening-autonomous-runs."]} />

## Разумный стартовый allowlist

Цель: заранее разрешить безопасные, повторяющиеся действия; держать разрушительные действия на **ask** или **deny**.

<Steps items={[{title: "Разрешите безопасное и повторяющееся", body: "Чтение файлов, запуск ваших команд тестирования/линтинга/сборки, git status/diff."}, {title: "Спрашивайте перед действиями среднего риска", body: "Установка зависимостей, запись файлов вне проекта, сетевые вызовы."}, {title: "Запретите разрушительное", body: "Чтение файлов с секретами (.env, файлы ключей), force-push, rm -rf."}]} />

Храните правила проекта в [`settings.json`](/docs/claude-code/settings) (общие), а личные переопределения — в `settings.local.json`.

<Callout type="tip" items={["Пусть учится на ваших запросах: подтвердите одну и ту же безопасную команду несколько раз — и вы точно будете знать, что добавить в ваш allowlist, превратив повторяющиеся запросы в одноразовое правило."]} />

<Callout type="takeaways" items={["Три вердикта: allow (без запроса), ask (по умолчанию — приостановиться и подтвердить), deny (никогда).", "Правила сопоставляются с инструментом плюс шаблоном, например Bash(npm run test:*) или Read(./.env).", "Четыре режима задают позицию сессии: default/ask, plan (только чтение), acceptEdits, bypass/yolo (только песочница).", "Составьте allowlist: разрешите безопасное и повторяющееся, спрашивайте для среднего риска, запрещайте разрушительное.", "Общие правила идут в settings.json; личные переопределения — в settings.local.json."]} />

<Quiz title="Проверь себя" questions={[{q: "Что делает вердикт по умолчанию 'ask' для действия, которое явно не разрешено и не запрещено?", options: ["Выполняет его молча", "Приостанавливается и просит вас подтвердить", "Блокирует его навсегда"], answer: 1, explain: "ask — значение по умолчанию для всего, что явно не разрешено/запрещено: он приостанавливается и спрашивает вас."}, {q: "Какой режим разрешений безопасен ТОЛЬКО в песочницах или CI, но никогда на машине с секретами?", options: ["plan", "acceptEdits", "bypass / yolo"], answer: 2, explain: "bypass / yolo полностью пропускает запросы; оставьте его для одноразовых сред — никогда на машине с секретами."}, {q: "Где следует хранить общие правила разрешений проекта по сравнению с личными переопределениями?", options: ["Оба в settings.local.json", "Правила проекта в settings.json; личные переопределения в settings.local.json", "Правила проекта в settings.local.json; личные переопределения в settings.json"], answer: 1, explain: "Правила проекта идут в settings.json (общие); личные переопределения идут в settings.local.json."}, {q: "Что из перечисленного относится к списку 'deny' в разумном стартовом allowlist?", options: ["git status / diff", "rm -rf и чтение .env", "Запуск ваших команд тестирования/линтинга/сборки"], answer: 1, explain: "Запрещайте разрушительные действия: чтение файлов с секретами (.env, файлы ключей), force-push и rm -rf."}]} />

## Дальше

- [settings.json: система конфигурации](/docs/claude-code/settings)
- [Хуки](/docs/claude-code/hooks) — обеспечивайте правила детерминированно, за пределами allow/deny
- [Безопасность и ответственное использование](/docs/security/responsible-use)
