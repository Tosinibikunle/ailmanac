---
sidebar_position: 6
title: "Permissões e Modos de Permissão"
description: "Regras allow/ask/deny e modos — reduza prompts sem perder segurança."
---

<LevelBadge level="intermediate" />

<VerifyNote lastVerified="2026-06-20" source="https://code.claude.com/docs/en/settings">
A sintaxe exata das regras e os nomes dos modos evoluem — confirme na documentação oficial de configurações/permissões do Claude Code.
</VerifyNote>

<Callout type="objectives" items={["O que significam os três veredictos de permissão (allow / ask / deny)", "Como uma regra de permissão associa uma ferramenta a um padrão", "Os quatro modos de permissão e quando usar cada um", "Como montar uma allowlist inicial sensata que reduz prompts sem perder segurança", "Onde armazenar as regras de permissão do projeto versus as pessoais"]} />

As permissões decidem **o que o Claude Code pode fazer sem parar para perguntar a você**. Ajuste-as bem e você obtém fluidez sem perder o controle; ajuste-as mal e você acaba aprovando tudo automaticamente ou se afogando em prompts.

## Os três veredictos

Toda ação potencial se resolve em um destes:

<Flashcards cards={[{front: "allow", back: "Faça, sem prompt."}, {front: "ask", back: "Pause e pergunte a você — o padrão para qualquer coisa não explicitamente permitida ou negada."}, {front: "deny", back: "Nunca, nem mesmo se for solicitado."}]} />

As regras geralmente associam uma ferramenta a um padrão, por exemplo, permitir `Bash(npm run test:*)` ou negar `Read(./.env)`.

<PromptCard title="Permitir uma ferramenta + padrão">{`Bash(npm run test:*)`}</PromptCard>

<PromptCard title="Negar uma ferramenta + padrão">{`Read(./.env)`}</PromptCard>

## Modos de permissão

Um modo define a postura geral de uma sessão:

| Modo | Comportamento | Use quando |
|---|---|---|
| **default / ask** | Pergunta para qualquer coisa não pré-permitida | Trabalho do dia a dia |
| **plan** | Somente leitura; propõe, nunca age | Tarefas grandes/arriscadas — veja [Modo Plano](/docs/claude-code/plan-mode) |
| **acceptEdits** | Aceita edições de arquivo automaticamente | Uma sessão de edição confiável e bem delimitada |
| **bypass / yolo** | Pula os prompts inteiramente | Apenas sandboxes/CI — **nunca** em uma máquina com segredos |

<Callout type="warning" items={["O bypass pertence a uma sandbox. Rodar com todos os prompts desligados na sua máquina real é como um agente acaba tocando em algo que não deveria. Reserve isso para ambientes descartáveis. Veja Fortalecendo Execuções Autônomas em /docs/security/hardening-autonomous-runs."]} />

## Uma allowlist inicial sensata

O objetivo: pré-permitir as coisas seguras e repetitivas; manter as coisas destrutivas em **ask** ou **deny**.

<Steps items={[{title: "Permita o seguro + repetitivo", body: "Ler arquivos, executar seus comandos de teste/lint/build, git status/diff."}, {title: "Pergunte antes do risco médio", body: "Instalar dependências, escrever arquivos fora do projeto, chamadas de rede."}, {title: "Negue o destrutivo", body: "Ler arquivos secretos (.env, arquivos de chave), force-push, rm -rf."}]} />

Armazene as regras do projeto em [`settings.json`](/docs/claude-code/settings) (compartilhado) e as substituições pessoais em `settings.local.json`.

<Callout type="tip" items={["Deixe-o aprender com seus prompts: aprove o mesmo comando seguro algumas vezes e você saberá exatamente o que adicionar à sua allowlist — transformando prompts repetidos em uma regra única."]} />

<Callout type="takeaways" items={["Três veredictos: allow (sem prompt), ask (padrão — pause e confirme), deny (nunca).", "As regras associam uma ferramenta a um padrão, como Bash(npm run test:*) ou Read(./.env).", "Quatro modos definem a postura da sessão: default/ask, plan (somente leitura), acceptEdits, bypass/yolo (apenas sandbox).", "Monte uma allowlist: permita o seguro + repetitivo, pergunte para o risco médio, negue o destrutivo.", "As regras compartilhadas vão em settings.json; as substituições pessoais em settings.local.json."]} />

<Quiz title="Teste-se" questions={[{q: "O que o veredicto padrão 'ask' faz para uma ação que não é explicitamente permitida nem negada?", options: ["Executa silenciosamente", "Pausa e pede que você confirme", "Bloqueia permanentemente"], answer: 1, explain: "ask é o padrão para qualquer coisa não explicitamente permitida/negada — ele pausa e pergunta a você."}, {q: "Qual modo de permissão é seguro APENAS em sandboxes ou CI, nunca em uma máquina com segredos?", options: ["plan", "acceptEdits", "bypass / yolo"], answer: 2, explain: "bypass / yolo pula os prompts inteiramente; reserve-o para ambientes descartáveis — nunca em uma máquina com segredos."}, {q: "Onde você deve armazenar as regras de permissão compartilhadas do projeto versus as substituições pessoais?", options: ["Ambas em settings.local.json", "Regras do projeto em settings.json; substituições pessoais em settings.local.json", "Regras do projeto em settings.local.json; substituições pessoais em settings.json"], answer: 1, explain: "As regras do projeto vão em settings.json (compartilhado); as substituições pessoais vão em settings.local.json."}, {q: "Qual destas pertence à lista 'deny' em uma allowlist inicial sensata?", options: ["git status / diff", "rm -rf e ler .env", "Executar seus comandos de teste/lint/build"], answer: 1, explain: "Negue as coisas destrutivas: ler arquivos secretos (.env, arquivos de chave), force-push e rm -rf."}]} />

## Próximos passos

- [settings.json: O Sistema de Configuração](/docs/claude-code/settings)
- [Hooks](/docs/claude-code/hooks) — imponha regras de forma determinística, além de allow/deny
- [Segurança e Uso Responsável](/docs/security/responsible-use)
