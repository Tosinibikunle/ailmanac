---
sidebar_position: 3
title: "把少样本示例用对"
description: "如何挑选、排序并格式化真正能教会任务的示例。"
---

<LevelBadge level="intermediate" />

<Callout type="objectives" items={["什么是少样本提示，以及为什么示例胜过描述", "如何读懂一条让模型来补完的清爽少样本提示", "如何挑选、格式化并排序示例（包括边缘情况）", "什么时候选用零样本，什么时候选用少样本", "为什么一个马虎的示例比没有示例更糟"]} />

**少样本提示**是指在让模型处理一个新例子之前，先给它看几个做好的任务范例。做得好的话，这是锁定某种格式、风格或边缘情况行为的最快方式——往往比用语言描述你想要什么更有效。

## 为什么示例胜过描述

"简洁而友好"很含糊。*展示*两份简洁、友好的输出则毫不含糊。模型会对这些示例进行模式匹配，并延续这个模式。

## 一条清爽的少样本提示词

注意它的形态：几组格式完全相同的 `Message → Label` 配对，然后是最后一条带着空 `Label:` 的消息，留给模型来填。

<PromptCard title="少样本分类提示词">{`Classify each support message as: billing, bug, or feature.

Message: "I was charged twice this month."
Label: billing

Message: "The app crashes when I upload a photo."
Label: bug

Message: "Can you add dark mode?"
Label: feature

Message: "My subscription renewed at the wrong price."
Label:`}</PromptCard>

模型掌握了这个模式；它会把最后一行补完。

## 如何挑选与格式化示例

<Steps items={[{title: "覆盖多样性——尤其是边缘情况", body: "把你在意的边缘情况包含进来。如果某个类别罕见或棘手，就把它包含进来。"}, {title: "保持示例格式一致", body: "相同的结构、相同的标签、相同的分隔符。不一致只会教出不一致。"}, {title: "用 2 到 5 个示例", body: "2 到 5 个往往就够了。对困难／多变的任务，多一些会有帮助，但会消耗 token，而且可能过拟合到示例的怪癖上。"}, {title: "有意识地排序", body: "顺序可能很重要——把最清晰的示例放在最前面；对于分类任务，不要把同一标签的示例全都聚在一起。"}, {title: "把示例与实时输入分开", body: "使用分隔符（或 XML 标签）把示例与你真正想要标注的输入分开。"}]} />

上面提到的 token 成本是实打实的——参见 [Token 与上下文](/docs/foundations/tokens-and-context)。至于分隔符做法，[XML 标签](/docs/prompting/xml-tags)是干净、可靠的分隔方式。

## 零样本对比少样本

先试试**零样本**（直接提问）——现代模型很强。当你需要某种**特定的格式／风格**，或任务**含糊不清**时，再加上示例。如果零样本已经做得很到位，就不必为示例买单。

<Callout type="tip" items={["示例就是数据——保持它们干净。一个错误或马虎的示例会主动教出错误的东西。要像对待训练数据一样去精心筛选它们。"]} />

<Callout type="takeaways" items={["展示，而非描述：做好的范例胜过含糊的描述。", "覆盖多样性，尤其是边缘情况，并让每个示例都保持完全相同的格式。", "2 到 5 个示例通常就够了；更多可能过拟合并消耗 token。", "顺序很重要——最清晰的放最前面，不要把同一标签聚在一起。", "从零样本开始；只在需要特定格式／风格或任务含糊时才加上示例。"]} />

<Quiz title="自我检测" questions={[{q: "为什么示例往往胜过像“简洁而友好”这样的文字描述？", options: ["它们比描述使用更少的 token", "它们毫不含糊——模型会进行模式匹配并延续这个模式", "它们会关闭模型的安全过滤器", "它们会强迫模型进入零样本模式"], answer: 1, explain: "像“简洁而友好”这样的描述很含糊；展示简洁、友好的输出则毫不含糊，模型会延续这个模式。"}, {q: "对一条少样本提示来说，多少个示例“往往就够了”？", options: ["正好 1 个", "2 到 5 个", "至少 10 个", "上下文能塞下多少就多少"], answer: 1, explain: "2 到 5 个往往就够了。对困难或多变的任务，多一些会有帮助，但会消耗 token，而且可能过拟合到示例的怪癖上。"}, {q: "对于分类任务，应该如何排序示例？", options: ["把同一标签的所有示例聚在一起", "把最清晰的示例放在最前面，并避免把同一标签聚在一起", "始终按标签字母顺序排列", "顺序根本无所谓"], answer: 1, explain: "顺序可能很重要——把最清晰的示例放在最前面，而且对于分类任务不要把同一标签的示例全都聚在一起。"}, {q: "什么时候应该优先选用零样本而不是少样本？", options: ["永远不该——总是要加上示例", "当零样本已经把任务做得很到位时，这样你就不必为示例买单", "只有在任务高度含糊时", "当你需要非常特定的输出格式时"], answer: 1, explain: "先试零样本；现代模型很强。如果它已经把任务做得很到位，就不必为示例买单。需要特定格式／风格或任务含糊时再加上示例。"}, {q: "一个马虎或错误的示例有什么风险？", options: ["它会被模型忽略", "它会主动教出错误的东西", "它只是浪费 token，但其余无害", "它通过加入噪声提升了鲁棒性"], answer: 1, explain: "示例就是数据——一个错误或马虎的示例会主动教出错误的东西。要像对待训练数据一样去精心筛选它们。"}]} />

## 下一步

- [提示词模式库](/docs/prompting/pattern-library)
- [用 XML 标签构建提示词结构](/docs/prompting/xml-tags)
- [可复用提示词模板](/docs/templates/prompts)
