---
sidebar_position: 6
title: "Permissions & Permission Modes"
description: "allow/ask/deny rules and modes — cut prompts without losing safety."
---

<LevelBadge level="intermediate" />

<VerifyNote lastVerified="2026-06-20" source="https://code.claude.com/docs/en/settings">
Exact rule syntax and mode names evolve — confirm against the official Claude Code settings/permissions docs.
</VerifyNote>

<Callout type="objectives" items={["What the three permission verdicts (allow / ask / deny) mean", "How permission rules match a tool plus a pattern", "The four permission modes and when to use each one", "How to build a sane starting allowlist that cuts prompts without losing safety", "Where to store project vs. personal permission rules"]} />

Permissions decide **what Claude Code can do without stopping to ask you**. Tune them well and you get flow without losing control; tune them badly and you either rubber-stamp everything or drown in prompts.

## The three verdicts

Every potential action resolves to one of:

<Flashcards cards={[{front: "allow", back: "Do it, no prompt."}, {front: "ask", back: "Pause and ask you — the default for anything not explicitly allowed or denied."}, {front: "deny", back: "Never, not even if asked."}]} />

Rules typically match a tool plus a pattern, e.g. allow `Bash(npm run test:*)` or deny `Read(./.env)`.

<PromptCard title="Allow a tool + pattern">{`Bash(npm run test:*)`}</PromptCard>

<PromptCard title="Deny a tool + pattern">{`Read(./.env)`}</PromptCard>

## Permission modes

A mode sets the overall posture for a session:

| Mode | Behaviour | Use when |
|---|---|---|
| **default / ask** | Prompts for anything not pre-allowed | Day-to-day work |
| **plan** | Read-only; proposes, never acts | Big/risky tasks — see [Plan Mode](/docs/claude-code/plan-mode) |
| **acceptEdits** | Auto-accepts file edits | A trusted, well-scoped editing session |
| **bypass / yolo** | Skips prompts entirely | Sandboxes/CI only — **never** on a machine with secrets |

<Callout type="warning" items={["Bypass belongs in a sandbox. Running with all prompts off on your real machine is how an agent ends up touching something it shouldn't. Reserve it for disposable environments. See Hardening Autonomous Runs at /docs/security/hardening-autonomous-runs."]} />

## A sane starting allowlist

The goal: pre-allow the safe, repetitive things; keep destructive things on **ask** or **deny**.

<Steps items={[{title: "Allow the safe + repetitive", body: "Read files, run your test/lint/build commands, git status/diff."}, {title: "Ask before the medium-risk", body: "Installing dependencies, writing files outside the project, network calls."}, {title: "Deny the destructive", body: "Reading secret files (.env, key files), force-push, rm -rf."}]} />

Store project rules in [`settings.json`](/docs/claude-code/settings) (shared) and personal overrides in `settings.local.json`.

<Callout type="tip" items={["Let it learn from your prompts: approve the same safe command a few times and you'll know exactly what to add to your allowlist — turning repeated prompts into a one-time rule."]} />

<Callout type="takeaways" items={["Three verdicts: allow (no prompt), ask (default — pause and confirm), deny (never).", "Rules match a tool plus a pattern, like Bash(npm run test:*) or Read(./.env).", "Four modes set session posture: default/ask, plan (read-only), acceptEdits, bypass/yolo (sandbox only).", "Build an allowlist: allow safe + repetitive, ask for medium-risk, deny destructive.", "Shared rules go in settings.json; personal overrides in settings.local.json."]} />

<Quiz title="Check yourself" questions={[{q: "What does the default 'ask' verdict do for an action that is not explicitly allowed or denied?", options: ["Runs it silently", "Pauses and asks you to confirm", "Blocks it permanently"], answer: 1, explain: "ask is the default for anything not explicitly allowed/denied — it pauses and asks you."}, {q: "Which permission mode is safe ONLY in sandboxes or CI, never on a machine with secrets?", options: ["plan", "acceptEdits", "bypass / yolo"], answer: 2, explain: "bypass / yolo skips prompts entirely; reserve it for disposable environments — never on a machine with secrets."}, {q: "Where should you store shared project permission rules versus personal overrides?", options: ["Both in settings.local.json", "Project rules in settings.json; personal overrides in settings.local.json", "Project rules in settings.local.json; personal overrides in settings.json"], answer: 1, explain: "Project rules go in settings.json (shared); personal overrides go in settings.local.json."}, {q: "Which of these belongs on the 'deny' list in a sane starting allowlist?", options: ["git status / diff", "rm -rf and reading .env", "Running your test/lint/build commands"], answer: 1, explain: "Deny destructive things: reading secret files (.env, key files), force-push, and rm -rf."}]} />

## Next

- [settings.json: The Config System](/docs/claude-code/settings)
- [Hooks](/docs/claude-code/hooks) — enforce rules deterministically, beyond allow/deny
- [Security & Responsible Use](/docs/security/responsible-use)
